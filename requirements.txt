# Core agent framework
langgraph>=0.6.0,<0.7
langchain-anthropic>=0.3.0

# Web server (deploy target: Railway/Fly.io/Render)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# HTTP + env
requests>=2.31.0
python-dotenv>=1.0.0

# Optional tool SDKs (only needed if you enable that tool)
praw>=7.7.0          # Reddit (free tier)
