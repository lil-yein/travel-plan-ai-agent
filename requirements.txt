# Core agent framework (verified: LangGraph 1.2.x in 2026)
langgraph>=1.2.0
langchain-anthropic>=0.3.0

# HTTP + env
requests>=2.31.0
python-dotenv>=1.0.0

# Optional tool SDKs (only needed if you enable that tool)
praw>=7.7.0          # Reddit (free tier)
